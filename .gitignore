node_modules
dist
.env
.env.local
.firebase/
firestore-debug.log
firebase-debug.log
*.log
documentation/

# Scratch/source icon files — not shipped with the app
icons/

# macOS
.DS_Store

# Claude Code local instructions
CLAUDE.md
.claude/

# Firebase iOS config — contains project identifiers, kept out of public repo
ios/App/App/GoogleService-Info.plist

# Cloud Functions — compiled output, deps, and local dev secrets
functions/lib/
functions/node_modules/
functions/.secret.local
